# Web 前端

本目录为 Vite 工程根（`vite.config.js` 中 `root: 'web'`）。

- 入口脚本：`src/main.js`
- 样式：`public/styles/main.css`（构建/开发均从站点根以 `/styles/main.css` 访问）
- 静态页：`index.html`

完整说明见仓库根目录 [README.md](../README.md)。

策略调节面板：`docs/strategy-dashboard.html`（若与当前 `DEFAULT_STRATEGIES` 字段不一致，以 `src/config.js` 为准）。
