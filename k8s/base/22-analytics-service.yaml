apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-service
  namespace: openblock
  labels:
    app.kubernetes.io/name: analytics-service
    app.kubernetes.io/part-of: openblock
spec:
  replicas: 1
  selector:
    matchLabels: { app.kubernetes.io/name: analytics-service }
  template:
    metadata:
      labels:
        app.kubernetes.io/name: analytics-service
        app.kubernetes.io/part-of: openblock
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8003"
        prometheus.io/path: "/metrics"
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: app
          image: ghcr.io/openblock/analytics:1.15.0
          ports:
            - { name: http, containerPort: 8003 }
          envFrom:
            - configMapRef: { name: openblock-config }
            - secretRef:    { name: openblock-secrets }
          env:
            - { name: SERVICE_PORT,     value: "8003" }
            - { name: OTEL_SERVICE_NAME, value: "openblock-analytics" }
          resources:
            requests: { cpu: "100m", memory: "128Mi" }
            limits:   { cpu: "1",    memory: "1Gi" }
          readinessProbe:
            httpGet: { path: /health, port: http }
          livenessProbe:
            httpGet: { path: /health, port: http }
            initialDelaySeconds: 15
            periodSeconds: 15
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
          volumeMounts: [ { name: tmp, mountPath: /tmp } ]
      volumes: [ { name: tmp, emptyDir: {} } ]
---
apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: openblock
spec:
  selector: { app.kubernetes.io/name: analytics-service }
  ports:
    - { name: http, port: 8003, targetPort: http }
